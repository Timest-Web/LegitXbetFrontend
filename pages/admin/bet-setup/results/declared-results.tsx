import DeclaredResults from "@/src/client/components/Admin/AdminDashboard/components/Bet Setup/Results/DeclaredResults";
import type { NextPage } from "next";

const DeclaredResult: NextPage = (props) => (
  <div>
    <DeclaredResults {...props} />
  </div>
);

export default DeclaredResult;
