import AllBets from "@/src/client/components/Admin/AdminDashboard/components/Bet Setup/Manage Bets/AllBets";
import type { NextPage } from "next";

const AllBet: NextPage = (props) => (
  <div>
    <AllBets {...props} />
  </div>
);

export default AllBet;
