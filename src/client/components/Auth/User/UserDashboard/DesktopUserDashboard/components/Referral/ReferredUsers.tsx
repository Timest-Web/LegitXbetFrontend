import LayoutDashboard from "../../shared/LayoutDashboard";
import ReferredUsersInner from "./ReferredUsersInner";

const ReferredUsers = () => {
  return <LayoutDashboard content={<ReferredUsersInner />} />;
};

export default ReferredUsers;
